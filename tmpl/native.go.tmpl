// Code generated by go-fuzz-build; DO NOT EDIT.

package {{.PkgName}}

import (
{{- range .Imports}}
	"{{.}}"
{{- end}}
	"os"
	"path/filepath"
	"testing"
)

func {{.FuncName}}_(f *testing.F) {
	loadTestCases(f)
	f.Fuzz(func(_ *testing.T, input {{.InputType}}) { {{.FuncName}}(input) })
}

func loadTestCases(f *testing.F) {
	dir, err := os.ReadDir(`{{.CorpusDir}}`)
	if err != nil {
		f.Fatal(err) // TODO: If not exists, show warning not adding any corpa
	}

	for _, corpus := range dir {
		if corpus.IsDir() {
			continue
		}

		input, err := os.ReadFile(filepath.Join(`{{.CorpusDir}}`, corpus.Name()))
		if err != nil {
			f.Fatal(err)
		}

{{- if gt .InputLen 0}}
		checkInputLength(input)
{{- end}}
		f.Add({{.InputCode}})
	}
}

{{- if gt .InputLen 0}}
func checkInputLength(input []byte) {
	if len(input) != {{.InputLen}} {
		panic(`invalid corpus: must be {{.InputLen}} bytes long for {{.InputType}} conversion`)
	}
}
{{- end}}
